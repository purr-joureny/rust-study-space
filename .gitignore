# 编译生成的文件
/target/
**/*.rs.bk

# Cargo 的缓存和构建文件
Cargo.lock

# 对于可执行项目，排除二进制文件（如果是库项目，可以保留 Cargo.lock）
# /<your-binary-name> (取消注释并替换为你的二进制名称)

# 排除 IDE 相关文件
.vscode/
.idea/

# 排除 Rust 的 `rustfmt` 和 `clippy` 的本地配置文件
rustfmt.toml
.clippy.toml

# 排除 macOS 的 `.DS_Store` 文件
.DS_Store

# 排除 `cargo audit` 的临时文件
Cargo.toml.bk

# 排除 `cargo` 的调试信息
*.d